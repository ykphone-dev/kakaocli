# Windows Standalone Probe Artifacts

Populate this directory by running the Windows probe executable on a real KakaoTalk installation.

Expected files:

- `environment.md`
- `paths.json`
- `db-schema.sql` or `db-schema.md`
- `uia-login-tree.json`
- `uia-chat-tree.json`
- `probe-verdict.md`

Do not mark Windows parity complete until these artifacts exist and support the claims in `.omx/fixtures/windows-parity/parity-signoff.md`.
